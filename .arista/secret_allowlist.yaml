# Arista Secret Scanner allow list (see go/secret-scanner for format and instructions)

version: v1.0
allowed_secrets:
- filepath_pattern: "vendor/.*(?!arista).*"
  category: FALSE_POSITIVE
  reason: vendored code not belonging to arista
