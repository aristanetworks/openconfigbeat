################### Openconfigbeat Configuration Example #########################

############################# Openconfigbeat ######################################

openconfigbeat:
  # Defines the addresses of the OpenConfig devices to connect to
  addresses: ["localhost:6042"]
  # Defines the OpenConfig paths to subscribe to
  paths: ["/"]
